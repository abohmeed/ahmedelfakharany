<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Explore top-rated DevOps courses, Kubernetes tutorials, and Linux training by an experienced instructor. Learn and grow your skills today!">
    <meta name="keywords" content="DevOps courses, Kubernetes tutorials, Linux training, Udemy courses">
    <meta name="author" content="Your Name">
    <title>My Courses | Learn DevOps, Kubernetes, and Linux</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .course-card {
            transition: transform 0.2s;
        }
        .course-card:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <header class="bg-dark text-white py-3">
        <div class="container">
            <h1 class="text-center">My Udemy Courses</h1>
            <p class="text-center">Enhance your skills with my top-rated courses on DevOps, Cloud, and Linux.</p>
        </div>
    </header>

    <main class="container py-5">
        <div class="row row-cols-1 row-cols-md-3 g-4">
            <!-- Example Course Card -->
{% for course in courses %}
    <div class="col">
        <a href="{{ course.link }}" target="_blank" class="text-decoration-none text-dark">
            <div class="card course-card">
                <img src="{{ course.image }}" class="card-img-top" alt="{{ course.title }}">
                <div class="card-body">
                    <h5 class="card-title">{{ course.title }}</h5>
                    <p class="card-text">{{ course.description }}</p>
                </div>
            </div>
        </a>
    </div>
{% endfor %}
        </div>
    </main>

    <footer class="bg-dark text-white py-3">
        <div class="container text-center">
            <p>&copy; 2025 Ahmed Elfakharany. All rights reserved.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
